module.exports = {

    // Información del bot
    bot: {
        name: "ALAN TX PRO",
        version: "1.0.0",
        prefix: "."
    },

    // Owner principal
    owner: {
        name: "Alan Store MX",
        lid: "@LID"
    },

    // Servidor
    server: {
        port: process.env.PORT || 3000
    },

    // Configuración general
    settings: {
        autoRead: false,
        autoTyping: true,
        public: true
    }

};
